# Web UI
gradio>=6.0
fastapi
uvicorn

# LLM
openai>=1.0

# 搜索 & 数据源
ddgs                              # DuckDuckGo 搜索（新包名；旧名 duckduckgo-search 会告警）
wikipedia-api
yt-dlp>=2024.0                    # 视频字幕（YouTube/Bilibili等）

# 爬虫
beautifulsoup4
lxml
httpx
tenacity                          # 重试机制

# 向量检索 & RAG
faiss-cpu
sentence-transformers
scikit-learn

# 数据模型 & 存储（用 stdlib dataclasses + sqlite3，无额外 ORM 依赖）

# 工具
python-dotenv

# 测试（可选，运行 python -m pytest tests -q 时需要）
pytest
